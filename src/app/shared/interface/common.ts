export interface TodoDetails {
    name: string
    emailId: string
    phoneNumber: string
}

export interface TodoList {
    name: string
    emailId: string
    phoneNumber: string
    id: string
}