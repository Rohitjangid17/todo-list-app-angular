export interface TodoDetails {
    name: string
    emailId: string
    phoneNumber: string
}

export interface TodoListId {
    name: string
    emailId: string
    phoneNumber: string
    id: string
}